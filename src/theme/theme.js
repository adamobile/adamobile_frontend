import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: 'rgb(176,35,39)',
    },
    secondary: {
      main: 'rgb(242,178,64)',
    },
  },
})

export default theme
